apiVersion: v1
kind: Service
metadata:
  name: {{ include "ap.name" . }}
  labels:
    {{- include "ap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ap.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
